spring.application.name=UniclouD_BackEnd
server.port=8081

# CONEXIÓN DATABASE POSTGRES
# =======================
spring.datasource.url=jdbc:postgresql://localhost:5432/unicloud_db
spring.datasource.username=unicloud
spring.datasource.password=claveunicloud
spring.datasource.driver-class-name=org.postgresql.Driver

spring.flyway.baseline-on-migrate=true

# =======================
# JPA / HIBERNATE
# =======================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
